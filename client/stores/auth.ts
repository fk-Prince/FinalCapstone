export interface SigninRequest {
    email: string;
    password: string;
}

export interface Signup {
    email: String,
    password: String,
    first_name: String,
    last_name: String,
    address: String,
    contact_number: String,
}